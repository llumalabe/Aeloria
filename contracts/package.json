{
  "name": "aeloria-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Aeloria game on Ronin Network",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network ronin-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network ronin-mainnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
